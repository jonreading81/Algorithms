import solution from '../src/maxSliceDouble';

describe('maxSliceDouble', function () {
  it('should solve codility example', function () {
    const A = [3,2,6,-1,4,5,-1,2];
    expect(solution(A)).to.equal(17);

  });

  it('should solve codility example', function () {
    const A = [3,2,6,-1,4,5,-1,2];
    expect(solution(A)).to.equal(17);

  });
});
